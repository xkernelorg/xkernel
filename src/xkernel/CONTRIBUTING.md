# Contributing to xkernel

## Development Setup
git clone https://github.com/YOURNAME/xkernel
cd xkernel
pip install -e .
pytest -q

## PR guidelines
- Keep kernel deterministic and minimal
- Add tests for new features
- Follow semantic versioning